{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认能力：dsh-app 仅需加载本地页面并访问本地网络",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "websocket:default"
  ]
}
